# PostgreSQL connection string (edit as needed)
#
# Option A (local Postgres, default 5432)
DATABASE_URL=postgresql://saudade@localhost:5432/tech_stack_overview
#
# Option B (Docker, see docker-compose.yml, host port 5433)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/tech_stack_overview

# JWT Secret for authentication (MUST change in production!)
JWT_SECRET=your-secret-key-change-in-production
